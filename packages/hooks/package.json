{
  "name": "@myapp/hooks",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "react": "^18.2.0"
  },
  "scripts": {
    "dev": "yarn run clean && yarn run watch",
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "tsc": "^2.0.3",
    "typescript": "^4.9.5"
  }
}
